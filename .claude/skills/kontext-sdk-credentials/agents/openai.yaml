interface:
  display_name: "Kontext Server Credentials"
  short_description: "Set up shared-token integrations and retrieve server-side credentials for a confidential app"
  default_prompt: "Use $kontext-sdk-credentials to set up a confidential app integration or retrieve the credential available to a platform user from the server side."
